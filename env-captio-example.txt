# Cuemark billing — Vercel env vars (keyweaver-backend)
# Dashboard → Settings → Environment Variables → redeploy after changes

NEXT_PUBLIC_SITE_URL=https://keyweaver.io
STRIPE_SECRET_KEY=sk_live_...

# Resend (transactional email) — improve inbox placement:
# 1. Resend dashboard → Domains → keyweaver.io must show Verified (SPF + DKIM)
# 2. Add DMARC in Cloudflare DNS (Resend shows the record)
# 3. Optional overrides (defaults: Cuemark <accounts@keyweaver.io>, reply-to hello@keyweaver.io):
# RESEND_FROM=Cuemark <accounts@keyweaver.io>
# RESEND_REPLY_TO=hello@keyweaver.io
# RESEND_API_KEY=re_...

# Webhook: Stripe → Developers → Webhooks
# URL: https://keyweaver-backend.vercel.app/api/captio/stripe-webhook
# Events: checkout.session.completed, invoice.paid, invoice.payment_failed, customer.subscription.deleted
STRIPE_CAPTIO_WEBHOOK_SECRET=whsec_...
# Legacy alias also supported:
# STRIPE_WEBHOOK_SECRET=whsec_...

# One Price ID per product (price_..., not prod_...). Stripe handles multi-currency on each price.
STRIPE_PRICE_CREDITS=price_...
STRIPE_PRICE_MONTHLY=price_...

# Diagnostic endpoint after deploy:
# GET https://keyweaver-backend.vercel.app/api/captio/billing-config

# Optional: allow captio_monthly tier in Supabase (run once)
# Supabase → SQL editor → paste section 4 from captio_auth_migration.sql
# Or set POSTGRES_URL on Vercel (Database → Connection string URI) plus:
# CAPTIO_INTERNAL_API_KEY=... then POST /api/captio/admin/migrate-tier with header x-captio-admin-key
#   → webhook_endpoint must match the URL configured in Stripe Dashboard
#   → webhook_secret_set must be true
#
# If payment succeeded but credits are missing:
# 1. Confirm Stripe webhook points to webhook_endpoint above (NOT /api/stripe/webhook)
# 2. Redeploy after setting STRIPE_CAPTIO_WEBHOOK_SECRET
# 3. Resend the checkout.session.completed event from Stripe → Developers → Events
# 4. Or open /account?session_id=cs_... while signed in (fulfills on load)
