# ── New env vars needed in Vercel for these routes ───────────────────────────
# Add these in Vercel dashboard → keyweaver-backend → Settings → Environment Variables

# Already exists (Keyweaver)
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
STRIPE_SECRET_KEY=
RESEND_API_KEY=

# New — add these
NEXT_PUBLIC_SITE_URL=https://keyweaver.io

# Stripe webhook secret — get from Stripe dashboard after registering the webhook
# Stripe → Developers → Webhooks → Add endpoint
# URL: https://keyweaver-backend.vercel.app/api/captio/stripe-webhook
# Events to listen for:
#   checkout.session.completed
#   invoice.paid
#   invoice.payment_failed
#   customer.subscription.deleted
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price IDs — get from Stripe dashboard → Products → Prices
# keyweaver_credits_499 product (one-time)
STRIPE_PRICE_CREDITS_GBP=price_...
STRIPE_PRICE_CREDITS_EUR=price_...
STRIPE_PRICE_CREDITS_USD=price_...

# keyweaver_monthly_1200 product (recurring)
STRIPE_PRICE_MONTHLY_GBP=price_...
STRIPE_PRICE_MONTHLY_EUR=price_...
STRIPE_PRICE_MONTHLY_USD=price_...
