/**
 * Cloudflare Turnstile site key (public — safe in static files).
 * Create a widget at https://dash.cloudflare.com → Turnstile
 * Hostnames: keyweaver.io, localhost
 *
 * Leave empty until configured; backend only enforces when TURNSTILE_SECRET_KEY is set on Vercel.
 */
window.CUEMARK_TURNSTILE_SITE_KEY = '';
